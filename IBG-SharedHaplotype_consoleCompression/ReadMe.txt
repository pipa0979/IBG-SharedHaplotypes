There are 2 flags (which are discussed in the readme file):
--in <filename.int>
--log <somefilename.log>
There's a makefile as well


To compile:
make all

To run:
./SharedHaplotype --in out1.pop1.gen20.chr10.int --log out1.pop1.gen20.chr10.log > output.gz
